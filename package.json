{
  "name": "intercampus",
  "version": "1.0.0",
  "description": "Full-stack internship management platform for connecting students with companies",
  "author": "zewo2, dmpl1909, goncalo842, cruxzit",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/zewo2/InterCampus-PAWA.git"
  },
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
